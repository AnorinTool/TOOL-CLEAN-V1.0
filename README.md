# TOOL-CLEAN-V1.0
🛠️ PHP Tool Manager (tool_clean)

Một công cụ mạnh mẽ và tối ưu Để Sử Dụng làm code đẹp và ngăn nắp

🚀 Hướng dẫn Dưới Đây:

Link Tool: curl -s https://github.com/AnorinTool/TOOL-CLEAN-V1.0/raw/refs/heads/main/install.sh | tr -d '\r' | bash

Xóa Tool Dưới Đây: 
rm -f $PREFIX/bin/tool_clean_v1.0
rm -f ~/bin/tool_clean_v1.0
hash -r

Lệnh Tool: tool_clean_v1.0
